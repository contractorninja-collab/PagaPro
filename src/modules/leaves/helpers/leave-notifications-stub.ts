/**
 * Future: enqueue NotificationOutbox rows for leave approvals / overlaps / balance warnings.
 * Kept isolated so worker integration does not entangle core workflow services.
 */
export function prepareLeaveNotificationPlaceholder(): void {
  /* intentionally empty — architecture hook */
}
