/** Mirrors `DocumentCategory` in Prisma — kept here so engine layers avoid coupling imports where unnecessary. */
export type DocumentCategory =
  | "CONTRACT"
  | "LEAVE"
  | "TERMINATION"
  | "WARNING"
  | "PAYROLL"
  | "OTHER";
