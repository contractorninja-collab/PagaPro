/**
 * Card chrome shared by the payroll detail tabs. These used to be duplicated
 * string constants inside the page component; the tabs now import them so a
 * section cannot drift into a different look by accident.
 */
export const CARD =
  "rounded-xl border border-[#e2e8f0] bg-white shadow-[0_1px_3px_rgba(15,23,42,0.05)]";

export const CARD_TITLE =
  "border-b border-[#eef2f7] px-5 py-3 text-[13px] font-bold text-[#0f172a]";
